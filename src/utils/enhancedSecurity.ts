
// Re-export from unified security for backward compatibility
export { 
  securityMonitor,
  unifiedValidation as validation,
  unifiedSanitizer as sanitizer,
  unifiedSecurityUtils as utils
} from './unifiedSecurity';
